name: Kirby cms 92i

# 🔁 Déclenchement du workflow à chaque push sur la branche main
on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest  # Le job tourne sur une machine virtuelle Ubuntu

    steps:
      # Étape 1 — Récupère ton code
      - name: Checkout repository
        uses: actions/checkout@v4

      # Étape 2 — Exécute un simple script
      - name: deploy
        run: docker compose up -d

      # Étape 3 — test integration
      - name: integration
        run: curl http://localhost
